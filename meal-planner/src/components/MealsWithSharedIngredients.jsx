export default function MealsWithSharedIngredients({
  addedMeals,
  mainIngredientsObj,
}) {
  const getMealsWithUsedIngredients = () => {
  };
  return (
    <div>
      {/* {sortedMealsWithUsedIngredients.map((meal, index) => {
        return <div key={index}>{meal.name}</div>;
      })} */}
    </div>
  );
}
